{% paginate search.results by 10 %}
  {% for item in search.results %}
    {% case item.object_type %}
      {% when 'product' %}
      {% render 'search-card', product: item %}
  {% else %}
    {% endcase %}
  {% endfor %}
  {% render 'pagination', pagination: paginate %}
{% endpaginate %}